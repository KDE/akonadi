CREATE TABLE CollectionAttributeTable (id int8 NOT NULL SERIAL PRIMARY KEY,
                                       collectionId int8 NOT NULL REFERENCES CollectionTable(id) ON DELETE CASCADE ON UPDATE CASCADE,
                                       type BYTEA NOT NULL,
                                       value BYTEA)
